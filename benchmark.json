{
  "schemaVersion": 1,
  "name": "mlxfast-challenge-dev",
  "description": "Optimize DeepSeek-V4-Flash MoE inference by transforming weights offline and reducing memory bandwidth without failing the correctness gate.",
  "category": "python",
  "direction": "-",
  "editablePaths": [
    "transform.py",
    "mlx_models"
  ],
  "setupCommand": ["bash", "-lc", "./setup.sh"],
  "benchmarkCommand": ["bash", "-lc", "./benchmark.sh"],
  "scorePath": "score.json",
  "runner": {
    "provider": "github-actions",
    "workflow": "benchmark.yml"
  }
}
